[pytest]
asyncio_mode = auto
markers =
    slow: network, large downloads, or slow local models
    e2e: full pipeline against real APIs (opt-in via RUN_E2E=1)
    eval: evaluation harness (mocked pipeline; fast)
